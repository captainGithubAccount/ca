<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="MatrixImageView">
        <!-- 连接线宽度 -->
        <attr name="fcLineWidth" format="dimension" />
        <!-- 缩放控制点半径 -->
        <attr name="fcScaleDotRadius" format="dimension" />

        <attr name="fcShapeType" format="integer" />
        <!-- 旋转控制点半径 -->
        <attr name="fcRotateDotRadius" format="dimension" />
        <!-- 图片控制框颜色 -->
        <attr name="fcFrameColor" format="color" />

        <attr name="showControlFrame" format="boolean" />
    </declare-styleable>


    <declare-styleable name="StatusImage">

        <attr name="ctrlStatus" format="boolean" />

        <attr name="ctrlImgOn" format="reference" />

        <attr name="ctrlImgOff" format="reference" />

    </declare-styleable>

    <attr name="popupTextAlignment" format="enum">
        <enum name="start" value="0" />
        <enum name="end" value="1" />
        <enum name="center" value="2" />
    </attr>

    <declare-styleable name="NiceSpinner">
        <attr name="arrowTint" format="color" />
        <attr name="hideArrow" format="boolean" />
        <attr name="arrowDrawable" format="reference|color" />
        <attr name="dropDownListPaddingBottom" format="dimension" />
        <attr name="backgroundSelector" format="integer" />
        <attr name="textTint" format="color" />
        <attr name="popupTextAlignment" />
        <attr name="entries" format="reference" />
    </declare-styleable>
</resources>